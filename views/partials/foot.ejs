    
    
    <section>
      <h2>Squads</h2>
      <ul>
        <% squads.forEach(squad => { %>
          <li><a href="/squad/<%= squad.id %>">Squad <%= squad.name %></a>, id <%= squad.id %>, tribe_id <%= squad.tribe_id %></li>
        <% }) %>
      </ul>
    </section>
  </body>
</html>